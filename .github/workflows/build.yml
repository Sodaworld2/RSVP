name: Deploy RSVP to Firebase Hosting

on:
  push:
    branches: [ feat/rsvp-app-with-game ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node 18
        uses: actions/setup-node@v4
        with:
          node-version: 18
          # ❌ do NOT set cache: npm unless you have package-lock.json

      - name: Install deps
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: rsvp-invite-7f77c
          # If this project has multiple hosting sites, also add:
          # site: rsvp-invite-7f77c
